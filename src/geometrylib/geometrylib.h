#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#define _USE_MATH_DEFINES
#include <math.h>


int checkCircleNum(char* line);

void structLetters(char* line);

int figureNameCheck(char* string, int n_line, char** structs);

int checkLine(char* line, int n_line);
